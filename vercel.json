{
  "functions": {
    "api/webhook.py": {
      "runtime": "python3.9"
    }
  },
  "routes": [
    { "src": "/api/webhook", "dest": "api/webhook.py" }
  ],
  "buildCommand": "pip install -r requirements.txt"
}
